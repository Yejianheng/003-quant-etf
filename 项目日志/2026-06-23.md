## 20:39 — 图表数据错误：创业板数据停滞 6-22

**现象**：`nav_2026.html` 图表中 159915 (创业板) 数据停在 2026-06-22，其余 4 只 ETF 已到 6-23。用户反馈"图表今天数据怎么又出错了"。

**排查路径**：
1. 直接查 5 只 ETF parquet 元信息 → 159915 确认为 3130 行、最新 6-22，其余 4 只 3131 行、最新 6-23
2. 跑 `nav_chart.py` 看日志 → 东方财富对 159915 连续 3 次 `ConnectionError: Remote end closed connection without response`
3. 新浪 fallback 触发但返回数据也只到 6-22 → 两个源都不可用
4. 验证文件时间戳 — `159915.parquet` 最后修改 Jun 22 22:10，确认今天未被更新
5. 探索替代源 → AKShare 只有 em/sina 两个 ETF 日线接口，无其他
6. 测 Baostock → 全部 5 ETF 有今日数据但历史仅 2026 年起（111 行），不足以做增量
7. 测 `ak.stock_zh_a_hist_tx`（腾讯财经）→ 全部 5 ETF 覆盖 2013 至今、今日数据齐全、200 正常返回

**根因**：东方财富 WAF 在 2026 年大幅升级反爬，`push2his.eastmoney.com` 直接断开连接（非超时，是主动拒绝）。新浪作为备用源存在半天级数据延迟，二者同时失效形成"双源盲区"。腾讯财经走的是交易所官方行情分发，不存在爬虫阻断问题。

**修复**：
- `src/data_pipeline.py`：新增 `fetch_etf_daily_tx`（腾讯财经主源），`amount→volume ×100` 转换，含拆分检测
- `scripts/update_data.py`：数据源优先级调整为 腾讯 → 东方财富 → 新浪，每源 3s 限流
- `scripts/nav_chart.py` + `scripts/check_position.py`：新增新鲜度门禁，任一 ETF 未到今日则 `RuntimeError`/`sys.exit(1)`，禁止生成图表
- 新增测试：`test_data_pipeline_tx.py`(6) + `test_freshness.py`(3) + 存量 mock 适配(3)

**教训**：
1. 两个爬虫源不构成冗余——东方财富和新浪的失效模式高度相关（都是免费网页抓取，无 SLA）。第三个源必须是不同性质的（腾讯是持牌信息商，接口稳定性有保障）。
2. 数据更新后必须验新鲜度。之前 silent failure（图表用旧数据"成功"生成）比报错更危险。门禁让问题可发现不可忽略。
3. AKShare 命名有误导：`stock_zh_a_hist_tx` 名为"A 股历史"，实际支持 ETF。不能只看函数名，要实测。

## 22:10 — 补充修改记录，全量回归测试

**操作**：4 文件补充 `[2026-06-23]` 修改记录，全量测试 `--ignore=tests/test_slippage.py` 403 passed / 6 failed（4 golden 偏移 + 2 已有 KeyError，均非回归）。

## 提交

| 提交 | 说明 |
|------|------|
| `v196-20260623-1` | 测试 — 腾讯财经数据源 + 新鲜度门禁 |
| `v197-20260623-2` | 新增 — 腾讯财经主数据源 fetch_etf_daily_tx |
| `v198-20260623-3` | 新增 — 新鲜度门禁，数据不齐禁止生成图表 |
| `v199-20260623-4` | 文档 — 补充修改记录，全量测试 403 通过 |
