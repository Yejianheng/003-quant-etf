## 2026-06-12 全天记录

### 1. 图表系统完善

**12:00-14:00**：T+1 执行延迟的图表逻辑澄清

**现象**：图表从 T+0 切换到 T+1 后，表格中权重和操作列逻辑混乱——权重显示当日 signal，操作显示信号变化，但 T+1 下今日持仓来自昨日 signal，明日调仓来自今日 signal。

**排查路径**：
1. 回测引擎 T+1 执行顺序 → 权重列应前移 1 天（来自昨日 signal），操作列应取当日 signal 比较
2. 首日缺前日 signal → 加 2025 末锚点行解决

**修复**：
- `nav_chart.py` `_build_table_data()`：权重 = `records_df.iloc[i-1]` 的 defense_active（昨日信号），操作 = 当日 signal vs 前日 signal
- `main()`：`records_2026` 前加一条 pre-2026 锚点行（不显示）
- 表头改为：「明日调仓」
- 空操作改为「无需调仓」（不用「—」）
- i=1（首日）操作 = 「建仓」

**教训**：T+1 下表格有两套独立的时间轴——权重是实际持仓（前日信号），操作是明日动作（当日信号），不能混为一套前移逻辑。

### 2. 夏普与策略极限分析

**16:00-19:00**：顾问与用户深入讨论夏普极限

**核心发现**：
- 策略无 alpha——纯 beta 开关控制，涨时参与跌时退出
- 夏普由分子的 ETF 收益和分母的波动率目标共同决定
- 数学对称性：-2.5 ↔ +2.6，底由 A 股涨跌停 + T+1 延迟 + 18% 止损共同封死
- A 股 ETF 涨跌停限制：510300/513100/518880/511010 = 10%，159915 = 20%
- 理论最坏：159915 连续 5 日跌停 -67%，但 ETF 分散数百股票，概率可忽略
- 现实极限：流动性地板 -25%/年（夏普 -2.5）

**教训**：不存在 alpha，只有 beta 的开关控制。关键是开关的准确性和响应速度。

### 3. 两个策略漏洞发现

**18:30-19:30**：

1. **sf 从未被应用**：`signal_generator.py` → `allocate_capital()` 只用 `drawdown_multiplier`，sf 算了但没传
2. **趋势过滤慢熊盲区**：`trend_strength > 0` 在 2018 年纳指缓慢阴跌时反复穿越 0

→ 已开 v181 direction，只读分析+独立测试，不修改生产代码。
