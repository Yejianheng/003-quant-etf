# 2026-05-28 项目日志

> 全天 8 commits（35→42），阶段 2 参数扫描 + 数据扩展 + 质量验证全部完成。

---

## 10:30 — 参数扫描基础设施改造

**现象**：回测引擎已跑通，但默认参数下绩效极差（Sharpe -0.01，最大回撤 -23.83%）。`parameter_scan()` 入口已存在，但 4 个硬编码参数阻塞全部 16 项扫描。

**改造**：
- A. `trend_threshold` 参数化（signal_generator.py:45 `ts > 0` → `ts > p["trend_threshold"]`）
- B. `drawdown_stop()` 自定义阈值（新增 `thresholds` 可选参数）
- C. `target_vol_alpha` 接入进攻层波动率缩放（进攻层原来只做等权，未缩放）
- D. `defense_ratio` 贯穿回测链路（allocate_capital 已接受参数但 run_backtest 未传）
- E. `parameter_scan()` checkpoint 持久化（断点续扫）

**教训**：写 direction.md 前必须先读源码——当时方向性讨论里的任务已经在工作区实现了（改过但未提交），写了冗余的 direction。浪费一轮检查才发现。

---

## 11:30 — 12 项参数扫描（2018-2025 基线）

**发现**：
- **trend_window=40 是唯一显著区分因子**：Sharpe 1.26，年化 10.4%，最大回撤 -7.5%
- 其余 11 项参数对结果几乎无影响（Sharpe 均为 -0.01）→ 进攻层空转，无行业 ETF 标的

**教训**：scan 2.3（momentum_short × momentum_long）需要配对扫描非笛卡尔积。`parameter_scan` 做 `itertools.product` 会产生 3×3=9 组合，但实际只需要 3 组配对。改用手动循环。

---

## 14:00 — 数据扩展至 2013-2025

**现象**：用户指出 2018-2025 只有 8 年，缺少 2015 股灾和 2016 熔断——A 股最重要的压力测试场景。

**排查**：
1. 东方财富 API 不可达（TLS 后 RST）→ 排除
2. 新浪 `fund_etf_hist_sina` 可用 → 拉 2012-2026 全量
3. 纳指 ETF 从 2013-07-31 开始（上市日），共同交易日起点受限

**结果**：5 文件 3115-3509 行，覆盖 2013-07-31 ~ 2026-05-28（3114 共同交易日）

**重跑扫描**：
- trend_window=40 仍最优：Sharpe 1.23（从 1.26 略降，2015 年波动更大）
- 基线 Sharpe 从 -0.01 → +0.22（2014-2015 牛市贡献）

---

## 16:00 — 策略 vs 三基准对比

| 指标 | 策略(trend=40) | 沪深300 | 创业板 | 纳指 |
|------|---------------|--------|--------|------|
| 总收益 | **203.8%** | 89.4% | 419.2% | 118.6% |
| 年化 | 9.8% | 4.9% | 12.6% | 6.5% |
| 波动率 | 8.0% | 22.2% | 33.1% | 31.3% |
| Sharpe | 1.23 | 0.22 | 0.38 | 0.21 |
| 最大回撤 | -10.4% | -46.3% | -69.6% | -85.5% |

**关键结论**：策略 8% 波动拿 10% 年化，Sharpe 是任何单一指数的 4-6 倍。创业板虽然总收益更高（419%），但 33% 波动和 -70% 回撤——风险调整后完败。

---

## 17:30 — 数据质量验证

**现象**：新浪源是东方财富不可达后的被动切换，质量从未验证。

**A1 基础完整性**：5 文件 6 项检查全 PASS（无负价、无重复日期、无 OHLC 逻辑违规、异常跳变仅纳指 1 次（美股假期复盘跳空））。

**A2 跨源验证**：东方财富不可达，baostock 不支持 ETF。采用指数点位回归：510300 ETF vs 沪深300 官方指数（`ak.stock_zh_index_daily`）。

| 比对项 | 结果 |
|--------|------|
| 价格相关性 | 0.999 |
| 日收益相关性 | 0.981 |
| 年化跟踪误差 | 4.72%（属 ETF 市场价正常水平，日噪声 × √252） |
| 13 年累计偏离 | 8.89%（年均 ~0.68%） |

**结论**：新浪数据源质量可靠，无系统偏差。

---

## 18:00 — 三基准固定对比改造

修改 `benchmark.py`（`compute_single_benchmark`）+ `backtest_engine.py`（返回新增 `benchmark_300`/`benchmark_chinext`/`benchmark_nasdaq`）+ `visualization.py`（图表新增三条虚线基准线）。新增 5 tests，全量 84 passed / 3 skipped。

---

---

## 19:00 — 顾问审查进攻层 direction.md + 补充步骤 0

**背景**：上一轮任务（数据扩展+质量验证+三基准）全部完成，direction.md 已写入新任务"进攻层行业 ETF 候选池构建"，但顾问审查发现缺少测试先行步骤。

**审查发现**：
1. `tests/test_etf_universe.py` 不存在，但 direction 涉及修改 `src/etf_universe.py` → 违反测试规范（`11-testing.md`）
2. `next-session.md` 过期——仍指向上一轮任务
3. direction 步骤 0 验收写"预期 0 passed, N failed"不准确——ETF_UNIVERSE 回归测试会绿，实际是"部分绿+部分红"

**修复**：
- direction.md 补充步骤 0（建测试文件+跑红灯），含测试场景清单（基础+边界+异常）
- 提交 v1-20260528-45
- 更新 next-session.md 至当前阶段

**当前状态**：direction.md 就位，等待执行窗口。

---

## 今日教训

1. **direction.md 写之前必先 git status**：否则不知道工作区已有未提交实现，写冗余任务。
2. **参数扫描的网格陷阱**：`itertools.product` 只适合独立参数，配对参数要手写循环。
3. **数据源验证是地基**：新浪源被动切换后拖了 20+ commits 才验证。以后换数据源第一步就做交叉比对。
4. **回测周期覆盖极端场景**：8 年数据看似够了，但缺 2015 股灾等于漏掉最重要的压力测试。
5. **direction 写完后顾问必须检查测试文件**：涉及 .py 文件时，`tests/test_xxx.py` 不存在则 direction 第一步必须是建测试。这次是新任务覆盖已有文件，但测试文件缺失——如果不是顾问审查拦下来，执行窗口会直接写 OFFENSE_POOL 跳过红灯。

1. **direction.md 写之前必先 git status**：否则不知道工作区已有未提交实现，写冗余任务。
2. **参数扫描的网格陷阱**：`itertools.product` 只适合独立参数，配对参数要手写循环。
3. **数据源验证是地基**：新浪源被动切换后拖了 20+ commits 才验证。以后换数据源第一步就做交叉比对。
4. **回测周期覆盖极端场景**：8 年数据看似够了，但缺 2015 股灾等于漏掉最重要的压力测试。
