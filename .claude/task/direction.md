# 执行指令

> 顾问写入。执行者只读、执行、写 outcome.md。

## 执行纪律（强制）

**每步完成 → 验证通过 → 提交 → 再进行下一步。** 禁止批量执行后统一提交。

---

## 步骤 1：Vol Target 触发审计

### 背景

Ablation 显示 vol target 在纯防御中 12 年零贡献。但这可能是"平均"掩盖了"瞬间"——vol target 可能在某些短暂窗口触发过，只是被平均后看不出来。

### 操作

写 `scripts/audit_vol_target.py`，扫描纯防御全量 2014-2026 回测的 records 数据：

1. 提取 `scaling_factor != 1.0` 的所有交易日（`scaling_factor` 在 signal 的 defense 字段中）
2. 如果 records 中没有存 scaling_factor，则修改引擎临时记录，重跑一遍全量

输出：

```
触发日期,scaling_factor,predicted_vol,active_count,熔断触发
2024-xx-xx,0.85,0.13,4,False
...
```

统计：

| 指标 | 值 |
|------|-----|
| 总交易日 | |
| sf != 1.0 的天数 | |
| 占比 | |
| sf 均值（触发时） | |
| sf 最小值 | |
| 触发期间 vs 全区间 Sharpe | |

### 验收

明确回答：纯防御 12 年 vol target 是否触发过、触发几次、效果如何。

---

## 步骤 2：极端场景压力测试（4.5 + 4.6）

### 背景

四 regime 已覆盖主要市场状态，但盲区未测：横盘慢性失血 + 极端流动性冲击。

### 2a：无趋势市场（4.6）

构造合成数据模拟 2 年横盘震荡。方法：从历史数据中提取 ETF 价格路径样本，叠加随机噪声使累计收益接近零，构造 2 年无趋势区间。

写 `scripts/stress_no_trend.py`，对纯防御在合成区间内回测：

| 指标 | 值 |
|------|-----|
| 年化收益 | |
| 最大回撤 | |
| Whipsaw 次数 | |
| Sharpe | |

核心问题：系统是否慢性失血？失血速度多快？

### 2b：极端流动性冲击（4.5）

用 2015 股灾和 2020 新冠数据构造"断层下跌"场景——连续 3-5 天跌停或接近跌停。纯防御在极端日的缩仓速度、峰值风险暴露。

写 `scripts/stress_liquidity_shock.py`：

| 指标 | 第一天 | 第二天 | 第三天 | 峰值 |
|------|--------|--------|--------|------|
| 风险暴露 | | | | |
| drawdown | | | | |
| 信号状态 | | | | |

### 验收

- 明确系统在两种极端场景下的存活能力和弱点
- 全量测试零回归

---

> 完成后写 outcome.md → 提示"请顾问窗口审查"。
