# 执行指令

> 顾问写入。执行者只读、执行、写 outcome.md。

## 执行纪律（强制）

**每步完成 → 验证通过 → 提交 → 再进行下一步。** 禁止批量执行后统一提交。

---

## 当前任务：P0/P1 补测（新增测试方案）

### 步骤 1：Golden Dataset（P0-1）

**目的**：建立永不变化的基准样本，任何引擎改动后结果完全一致。

**操作**：
1. 固定数据：defense 5 只 ETF 2018-01-01 ~ 2022-12-31（5 年，覆盖牛熊震荡）
2. 固定参数：`trend_window=40, ewma_lambda=0.94, target_vol_beta=0.10, corr_threshold=0.0, defense_ratio=1.0`
3. 固定初始资金：1,000,000
4. 运行纯防御回测，保存为 golden 基准：

| 输出 | 文件 |
|------|------|
| NAV 序列 | `output/golden_nav.csv` |
| 仓位序列 | `output/golden_positions.csv` |
| 信号序列 | `output/golden_signals.csv` |
| 交易记录 | `output/golden_trades.csv` |

5. 写 `tests/test_golden_dataset.py`：加载 golden 文件，重新运行回测，对比 NAV/positions/signals/trades 逐行一致。

**验收**：golden 测试通过（回测结果与基准完全一致）。之后任何引擎改动必须 golden 绿灯才能提交。

### 步骤 2：生存者偏差审计（P0-3）

**目的**：确认任意日期只能使用当时已上市的 ETF。

**操作**：
1. 查五只防御 ETF 的上市日期：

| ETF | 代码 | 上市日期 |
|-----|------|---------|
| 沪深300 | 510300 | 2012-05-28 |
| 创业板 | 159915 | 2011-09-22 |
| 纳指 | 513100 | 2013-05-15 |
| 黄金 | 518880 | 2013-06-24 |
| 国债ETF | 511010 | 2013-06-18 |

2. 验证：回测起始日（当前为各 ETF 全部就位后的第一个共同交易日）≥ 最后一只 ETF 上市日期
3. 验证 `get_available_etfs()` 的 `min_history` 检查不会引入尚不存在的 ETF
4. 检查：数据目录下是否有退市 ETF 的残留 parquet 文件

**验收**：回测全程没有使用未上市 ETF 的数据，`get_available_etfs` 逻辑正确。

### 步骤 3：熔断阈值敏感性 / 回撤止损阈值扫描（P1-8）

**目的**：确认 liquidate 阈值不是参数巧合。

**操作**：
纯防御全量 2014-2026，扫描 `dd_threshold_liquidate`：

| 阈值 | 预期 |
|------|------|
| 0.15 | 更早清仓，回撤更小，但可能干扰正常交易 |
| 0.18（当前） | 基准 |
| 0.20 | 硬约束上限 |
| 0.25 | 超过硬约束，作为压力参考 |

对比：

| 阈值 | Sharpe | 年化 | 最大回撤 | liquidate 触发次数 | 恢复天数 |
|------|--------|------|---------|------------------|---------|
| 0.15 | | | | | |
| 0.18 | | | | | |
| 0.20 | | | | | |
| 0.25 | | | | | |

**验收**：确认 0.18 在合理区间内，策略对阈值不敏感。

---

> 全部完成后写 outcome.md → 提示"请顾问窗口审查"。
