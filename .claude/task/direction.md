# 执行指令

> 顾问写入。执行者只读、执行、写 outcome.md。

## 执行纪律（强制）

**每步完成 → 验证通过 → 提交 → 再进行下一步。** 禁止批量执行后统一提交。

## 当前任务：子模块 ablation — 确认每个组件独立贡献

### 背景

纯防御 Sharpe 1.23 是四个模块叠加的结果：趋势过滤 + 波动率缩放 + EWMA 协方差 + 相关性熔断。但不知道每个模块独立贡献了多少。Ablation 逐个拆解。

### 四组对照

| 编号 | 测试 | 对照 | 观察 |
|------|------|------|------|
| 1.2 | 有 MA40 趋势过滤 | vs 无趋势过滤（全仓等权） | 回撤改善、熊市保护、假突破损耗 |
| 1.3 | 有 vol target | vs 固定权重（等权） | Sharpe 提升、波动率稳定性 |
| 1.4 | EWMA 协方差 | vs 简单历史协方差 | 风险响应速度、危机缩仓速度 |
| 1.5 | 有相关性熔断 | vs 无熔断 | 股债同跌阶段表现 |

### 步骤

#### 步骤 1：趋势过滤 ablation（1.2）

修改 `src/signal_generator.py` 的 `DEFAULT_PARAMS`，临时关掉趋势过滤（`trend_threshold` 设负值使所有 ETF 恒通过），对比：

| 指标 | 有趋势过滤 | 无趋势过滤 | 差异 |
|------|-----------|-----------|------|
| 总收益 | | | |
| 最大回撤 | | | |
| Sharpe | | | |
| 2018 年收益 | | | ← 熊市专项 |
| 2022 年收益 | | | ← 熊市专项 |
| Whipsaw 次数 | | | |

跑全量回测（2014-2026），确保全量测试不退化。改回原参数后提交。

#### 步骤 2：波动率目标 ablation（1.3）

临时设为固定权重（所有 active ETF 等权，不缩放），对比有 vol target：

| 指标 | 有 vol target | 固定等权 | 差异 |
|------|-------------|---------|------|
| Sharpe | | | |
| 波动率稳定性（年波动率方差）| | | |
| 极端回撤 | | | |
| 牛市参与率 | | | |

改回原参数后提交。

#### 步骤 3：EWMA 协方差 ablation（1.4）

临时替换 EWMA 为简单历史协方差（等权窗口），对比：

| 指标 | EWMA λ=0.94 | 历史协方差 | 差异 |
|------|------------|-----------|------|
| 危机缩仓速度（2020/2022）| | | |
| 组合预测误差 | | | |

改回原参数后提交。

#### 步骤 4：相关性熔断 ablation（1.5）

临时关掉熔断（`corr_threshold` 设极大值），对比：

| 指标 | 有熔断 | 无熔断 | 差异 |
|------|--------|--------|------|
| 2022 年收益 | | | ← 股债双杀 |
| 最大回撤 | | | |
| 熔断触发次数 | | | |

改回原参数后提交。

#### 步骤 5：汇总

输出 `output/ablation_summary.csv`：

| 模块 | 有模块 Sharpe | 无模块 Sharpe | ΔSharpe | 有模块回撤 | 无模块回撤 | 结论 |
|------|-------------|-------------|---------|----------|----------|------|

### 验收标准

- 四个 ablation 全部完成，每项独立提交
- 全量测试零回归
- 明确每个模块的边际贡献和不可替代性
- 不改引擎逻辑，仅通过参数开关控制

---

> 完成后写 outcome.md → 提示"请顾问窗口审查"。
