# 基建与环境约束

## 数据库

- 所有表结构变更必须通过 Migration 脚本执行，严禁直接 `ALTER TABLE`。
- 禁止在业务代码中拼接 SQL 字符串。
- [其他数据库约束]

## 第三方服务

- 调用外部 API 必须包裹在 `try-catch` 中。
- 必须统一使用 [指定的请求库/代理模块]。
- [其他第三方约束]

## 环境变量

- 新增环境变量必须同步在 `.env.example` 中记录。
- 禁止在非配置文件中硬编码凭证。
- [其他环境约束]
