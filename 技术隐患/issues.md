@claude-override-approved
# 待解决隐患

> 按重要性降序。Hook 级 = 需在 Hook 层加入强制检查，防止同类问题再现。

| # | 隐患 | 级别 | 状态 |
|---|------|:--:|:--:|
| 1 | Hook 无角色感知——顾问/执行角色隔离全靠 AI 遵守文字约定，Hook 层零参与。需增加角色门禁：会话启动写入角色标记文件，pre_edit_file.js / pre_bash.js 读取标记，顾问角色直接拦截所有 Edit/Write/Bash | **Hook 级** | 待解决 |
| 2 | 候选池筛选无标准行业分类——名称关键词推断行业不可靠，须引入申万/中信标准分类，筛（过滤不合格）与选（同行业内排名）必须分离 | **Hook 级** | 待解决 |
| 3 | 上市年限门槛太松——"≥2 年"导致 G3 代 ETF（3.5 年历史）栓死回测窗口，应为"先定回测窗口再反向筛标的，不达标暂不进进攻池" | 普通 | 待解决 |
| 4 | 同行业内选标规则缺失——当前按 AUM 最大，但 AUM 大 ≠ 动量信号好 ≠ 行业代表性强 | 普通 | 已解决 |
| 5 | check_offense_pool() 校验缺口——仅校验 candidates 代码为纯数字，不校验代表 code 是否在 candidates 列表中，也不校验 candidates 跨风险源是否重复。两项校验缺失意味着可以通过不一致的 OFFENSE_POOL 写入，审计层不拦截 | **Hook 级** | 待解决 |
